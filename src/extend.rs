pub fn extend_mode(
    (command, parallel, threads_number, max_length, args): (
        String,
        bool,
        Option<usize>,
        usize,
        impl Iterator<Item = String>,
    ),
) {
    unimplemented!()
}
