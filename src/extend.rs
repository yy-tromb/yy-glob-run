use crate::Config;

pub fn extend_mode(
    Config {
        command,
        parallel,
        threads_number,
        max_extend_length: _,
        stdout_label,
        stderr_label,
        ignore_error,
        args,
    }: Config,
) {
    unimplemented!()
}
